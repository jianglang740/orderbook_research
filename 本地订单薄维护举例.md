# 流程举例


**我以一个具体的、简化的示例来演示整个订单簿重建过程。为了便于理解，假设订单簿只有买3档、卖3档，并且事件ID的序列号只有三位数。模拟币安期货的REST快照和后续几条WebSocket增量消息，然后展示如何对齐、验证连续性，并更新本地订单簿。**



- 先订阅ws消息，开始缓存
- 初始状态应该为REST 快照，所以拉取 REST 快照，得到：

```json
{
  "lastUpdateId": 100,
  "bids": [
    ["50000.00", "1.2000"],
    ["49990.00", "2.0000"],
    ["49980.00", "0.5000"]
  ],
  "asks": [
    ["50010.00", "1.5000"],
    ["50020.00", "1.0000"],
    ["50030.00", "0.8000"]
  ]
}

```
**含义：**

- 当前快照已经包含了所有事件 ID ≤ 100 的订单簿变化。

- 当前买盘：价格 50000 有 1.2 BTC，价格 49990 有 2.0 BTC，价格 49980 有 0.5 BTC,卖盘类似于买盘。

**此时本地订单簿状态：**

- bids = {50000:1.2, 49990:2.0, 49980:0.5}
- asks = {50010:1.5, 50020:1.0, 50030:0.8}
- last_update_id = 100

WebSocket 增量消息（按接收顺序）
- 假设我们订阅后，先缓存了几条消息（因为对齐前不处理），且币安期货是稀疏推送，消息之间可能跳过一些 ID，但每条消息会携带其包含的 ID 区间。

消息 1（缓存中第一条）

```json
{
  "U": 101,
  "u": 103,
  "pu": 100,    // 表示上一条消息的 u 是 100（与快照的 lastUpdateId 吻合）
  "b": [
    ["50000.00", "1.5000"],   // 买价 50000 数量变为 1.5
    ["49970.00", "0.3000"]    // 新增买价 49970 数量 0.3
  ],
  "a": [
    ["50010.00", "0.0000"]    // 卖价 50010 数量归零（撤销该档位）
  ]
}
```

这条消息包含了事件 ID 101、102、103（共 3 个事件）：

- 事件101：修改 50000 数量从 1.2 → 1.5

- 事件102：新增 49970 买价 0.3

- 事件103：删除 50010 卖价（数量设为 0）

消息 2（缓存中第二条）

```json
{
  "U": 104,
  "u": 104,
  "pu": 103,
  "b": [
    ["49990.00", "0.0000"]    // 买价 49990 数量归零（撤销）
  ],
  "a": []    // 无卖盘变化
}
```

这条消息只包含一个事件 ID 104：撤销买价 49990。

消息 3（缓存中第三条）

```json
{
  "U": 106,
  "u": 107,
  "pu": 104,    // 注意：跳过了 ID 105！因为期货稀疏推送，某些事件被合并或未单独推送
  "b": [],
  "a": [
    ["50020.00", "2.0000"]    // 卖价 50020 数量从 1.0 增加到 2.0
  ]
}
```

- 包含事件 ID 106、107：将 50020 卖盘数量从 1.0 改为 2.0（可能是两个增量事件，但合并推送）。
- 这里 U=106，pu=104，说明事件105没有被推送（可能无实质变化，或合并到了后面的消息中）。

**对齐流程（主线程）**
- 步骤1：

```
拉取快照后，snap_id = 100

我们需要找一条消息，其 U <= 101 <= u（因为snap_id 是快照的 lastUpdateId，表示快照包含的最后一个事件ID。快照本身已经包含了所有事件ID ≤ snap_id 的更改。因此第一条需要应用的增量消息应该从 snap_id + 1 开始，即下一个事件ID。所以我们要找的增量消息必须覆盖到 snap_id + 1 这个ID，且其起始U ≤ snap_id+1 ≤ 结束u）
```


- 步骤2：

```
丢弃所有 u <= 100 的消息
消息1的 u=103 > 100 → 保留
消息2的 u=104 > 100 → 保留
消息3的 u=107 > 100 → 保留
```

- 步骤3：

```
遍历保留的消息，找满足条件的
消息1：U=101, u=103，检查 101 <= 101 <= 103 ✅ 符合。
应用消息1，更新订单簿。
应用后本地状态：

买盘：50000 变为 1.5；新增 49970:0.3；49990 维持 2.0（未改）；49980 维持 0.5
bids = {50000:1.5, 49990:2.0, 49980:0.5, 49970:0.3}

卖盘：删除 50010（原 1.5） → asks = {50020:1.0, 50030:0.8}
设置 last_u = 103，initialized = True。

然后中断循环（只处理第一条对齐消息）。
```

- 步骤4：

```
主循环继续处理队列中剩余的消息

接下来处理消息2（从队列中取出）：
|
检查连续性：msg["pu"] = 103 等于 last_u=103 ✅ 连续。
|
应用消息2：b 中有 ["49990.00", "0.0000"] → 删除买价 49990。
更新后 bids 删除 49990，剩余 {50000:1.5, 49980:0.5, 49970:0.3}
卖盘无变化。
|
更新 last_u = 104


处理消息3：
|
检查连续性：msg["pu"] = 104 等于 last_u=104 ✅ 连续（尽管跳过了 ID105，但 pu 正确地指向上一条消息的 u，这是期货允许的）。
|
应用消息3：a 中有 ["50020.00", "2.0000"] → 将 50020 数量从 1.0 改为 2.0。
更新后 asks = {50020:2.0, 50030:0.8}
|
更新 last_u = 107
```


**最终订单簿状态:**

```
bids（买盘，从高到低）：
50000 → 1.5
49980 → 0.5
49970 → 0.3
（49990 已被撤销）

asks（卖盘，从低到高）：
50020 → 2.0
50030 → 0.8
（50010 已被撤销）
````

